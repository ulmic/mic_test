= simple_form_for @variant, url: admin_variants_path, html: { class: 'form-horizontal' } do |f|
  = f.input :question_id
  = f.input :text
  .form-actions
    = f.button :submit, class: 'btn-primary'
    = link_to t('.cancel', default: t("helpers.links.cancel")), admin_variants_path, class: 'btn'
